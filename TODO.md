# TODO: Change Export to Player Stats

## Steps to Complete

- [x] Modify export_data_page in app.py to calculate aggregated player stats using StatsCalculator
- [x] Change JSON export to export batting_stats and pitching_stats instead of games data
- [x] Change CSV export to provide separate downloads for batting stats CSV and pitching stats CSV
- [x] Update export summary metrics if needed
- [x] Test the export functionality to ensure player stats are exported correctly
